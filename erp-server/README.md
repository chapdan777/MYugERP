# Production ERP Server

Серверное приложение для управления производственным предприятием, построенное на NestJS с использованием принципов Clean Architecture и Domain-Driven Design (DDD).

## 🎯 Основные возможности

- **Управление заказами** - полный цикл работы с клиентскими заказами
- **Производственный процесс** - заказ-наряды, канбан-доски, контроль приоритетов
- **Управление работниками** - квалификации, назначения, расчет зарплаты
- **Бухгалтерия** - учет платежей, баланс клиентов, распределение оплат
- **Аудит** - полное логирование всех операций
- **Real-time обновления** - WebSocket для мгновенных уведомлений

## 🚀 Производительность и оптимизации

### 🔧 Оптимизация базы данных (PostgreSQL в Docker)

**Результаты оптимизации:**
- **Среднее время отклика API:** ↓ с 1.7ms до 0.5ms (улучшение на 70%)
- **Максимальное время отклика:** ↓ с 106ms до 23ms (улучшение на 78%)
- **Cache hit ratio:** 75.93%
- **Использование ресурсов:** CPU 0.03%, память 31MB

**Реализованные оптимизации:**
- ✅ Оптимизированная конфигурация PostgreSQL для Docker
- ✅ Критические индексы для таблицы `price_modifiers`
- ✅ Автоматическая система мониторинга производительности
- ✅ Регулярные backup'ы базы данных

### 📊 Мониторинг производительности

**Автоматизированные скрипты:**
```
# Мониторинг ресурсов и производительности
./scripts/monitor-postgres.sh

# Ежедневные backup'ы базы данных
./scripts/postgres-backup.sh

# Создание оптимизированных индексов
./scripts/create-optimized-indexes.sh
```

**Метрики для отслеживания:**
- Время отклика API < 100ms
- Использование CPU < 70%
- Использование памяти < 80%
- Cache hit ratio > 90%

### 🐳 Docker развертывание

**Production-ready конфигурация:**
- Оптимизированные параметры PostgreSQL
- Health checks для всех сервисов
- Автоматическое восстановление после сбоев
- Безопасная конфигурация сети

**Подробное руководство по развертыванию:**
См. [`PRODUCTION-DEPLOYMENT-RU.md`](./PRODUCTION-DEPLOYMENT-RU.md)

## 🏗️ Технологический стек

- **Runtime:** Node.js LTS
- **Framework:** NestJS LTS
- **Language:** TypeScript (strict mode)
- **Database:** PostgreSQL
- **ORM:** TypeORM
- **Authentication:** JWT (access + refresh tokens)
- **Validation:** class-validator
- **Testing:** Jest
- **Real-time:** WebSocket (Socket.io)

## 📁 Архитектура

Проект следует принципам **Clean Architecture** с четким разделением на слои:

```
src/
├── modules/              # Модули доменов (ограниченные контексты)
│   ├── orders/          # Управление заказами
│   ├── products/        # Каталог продукции
│   ├── properties/      # Система свойств
│   ├── pricing/         # Ценообразование
│   ├── users/           # Пользователи
│   ├── configuration/   # Конфигурация (шаблоны)
│   ├── production/      # Производство (технологические маршруты)
│   ├── work-orders/     # Заказ-наряды
│   ├── workforce/       # Трудовые ресурсы
│   ├── accounting/      # Бухгалтерия
│   └── audit/           # Аудит
├── common/              # Общие утилиты
│   ├── config/          # Конфигурация
│   ├── decorators/      # Декораторы
│   ├── guards/          # Guards
│   ├── interceptors/    # Interceptors
│   ├── filters/         # Exception filters
│   └── utils/           # Утилиты
├── core/                # Ядро приложения
└── migrations/          # Миграции БД
```

Каждый модуль организован по слоям:
- **domain/** - доменная логика (сущности, value objects, сервисы)
- **application/** - use cases, порты, DTO
- **infrastructure/** - реализация репозиториев, ORM entities
- **presentation/** - контроллеры, валидация

## 🚀 Быстрый старт

### Требования

- Node.js >= 18.x LTS
- PostgreSQL >= 14.x
- npm >= 8.x

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd erp-server

# Установить зависимости
npm install --legacy-peer-deps

# Скопировать файл окружения
cp .env.example .env

# Настроить переменные в .env (особенно DATABASE_*)
```

### Настройка базы данных

```bash
# Создать базу данных
createdb erp_production

# Запустить миграции
npm run migration:run
```

### Запуск

```bash
# Development mode (hot reload)
npm run start:dev

# Production mode
npm run build
npm run start:prod
```

Сервер будет доступен по адресу: `http://localhost:3000/api`

## 📝 Скрипты

```
npm run build          # Сборка проекта
npm run start          # Запуск в обычном режиме
npm run start:dev      # Запуск в dev режиме (hot reload)
npm run start:prod     # Запуск в production
npm run lint           # Проверка кода
npm run test           # Юнит-тесты
npm run test:e2e       # E2E тесты
npm run test:cov       # Покрытие тестами

# Работа с миграциями
npm run migration:generate -- MigrationName  # Создать миграцию
npm run migration:run                        # Применить миграции
npm run migration:revert                     # Откатить последнюю миграцию
```

## 🔐 Переменные окружения

Основные переменные в `.env`:

```
# Приложение
NODE_ENV=development
PORT=3000

# База данных
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=erp_production
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=true

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRATION=15m
REFRESH_TOKEN_SECRET=your-refresh-secret
REFRESH_TOKEN_EXPIRATION=7d

# Логирование
LOG_LEVEL=debug
```

## 📚 API Документация

После запуска сервера:
- **Swagger UI (интерактивная документация):** `http://localhost:3000/api/docs`
- **REST API базовый адрес:** `http://localhost:3000/api`
- **WebSocket:** `ws://localhost:3000`

Основные endpoints:
- `/api/auth` - Аутентификация (login, refresh, logout, me)
- `/api/users` - Управление пользователями
- `/api/products` - Управление продуктами (номенклатура)
- `/api/properties` - Управление свойствами продуктов
- `/api/property-dependencies` - Зависимости свойств
- `/api/price-modifiers` - Модификаторы цен
  - `POST /price-modifiers` - Создать модификатор цены
  - `GET /price-modifiers` - Получить список модификаторов
  - `GET /price-modifiers/:id` - Получить модификатор по ID
  - `PUT /price-modifiers/:id` - Обновить модификатор
  - `DELETE /price-modifiers/:id` - Удалить модификатор
  - `POST /price-modifiers/calculate` - Рассчитать цену с учетом модификаторов
- `/api/order-templates` - Шаблоны заказов
- `/api/orders` - Управление заказами
- `/api/work-orders` - Заказ-наряды и производственные задания
- `/api/kanban` - Канбан-доски
- `/api/payments` - Управление платежами
- `/api/client-balances` - Балансы клиентов

> 💡 Для тестирования API используйте Swagger UI по адресу `/api/docs`

## 🧪 Тестирование

```
# Юнит-тесты
npm run test

# E2E тесты
npm run test:e2e

# Покрытие
npm run test:cov
```

## 🏛️ Принципы разработки

### Clean Architecture
- Зависимости направлены внутрь: presentation → application → domain
- Доменный слой не зависит от внешних фреймворков
- Изоляция модулей - отсутствие внутренних зависимостей

### Domain-Driven Design
- Ограниченные контексты (Bounded Contexts)
- Агрегаты и корни агрегатов
- Value Objects для инкапсуляции логики
- Доменные события для межмодульной коммуникации

### SOLID Принципы
- Single Responsibility
- Open/Closed
- Liskov Substitution
- Interface Segregation
- Dependency Inversion

## 📋 Фазы реализации

Проект разделен на 6 основных фаз:

1. **Фаза 1: Фундамент** (Недели 1-4)
   - Настройка проекта, БД, аутентификация, пользователи

2. **Фаза 2: Управление заказами** (Недели 5-8)
   - Номенклатура, свойства, ценообразование, заказы

3. **Фаза 3: Производственный процесс** (Недели 9-12)
   - Заказ-наряды, канбан, технологические маршруты

4. **Фаза 4: Трудовые ресурсы** (Недели 13-15)
   - Назначения работников, расчет зарплаты

5. **Фаза 5: Бухгалтерия** (Недели 16-18)
   - Платежи, балансы, распределение оплат

6. **Фаза 6: Тестирование** (Недели 19-20)
   - Полное покрытие тестами, оптимизация, безопасность

## 📄 Лицензия

UNLICENSED - внутренний проект

## 👥 Команда

MassivYug Production Team

---

**Важно:** Этот проект использует TypeScript strict mode и следует строгим архитектурным принципам для обеспечения поддерживаемости и масштабируемости.
